/* Main Site Layout */

.layout {
    @apply w-full h-full relative;
}

.main {
    @apply flex flex-col lg:p-16 md:p-8 p-4 overflow-hidden;
}

body {
    @apply dark:bg-bg-dark bg-bg overflow-x-hidden dark:text-white;
    transition: color, background-color 0.2s ease-in;
}
