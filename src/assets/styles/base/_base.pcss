/* Main Site Layout */

.layout {
    @apply w-full h-full relative;
}

.main {
    @apply flex flex-col lg:m-16 md:m-8 m-4;
}

body {
    @apply bg-bg overflow-x-hidden;
    transition: color, background-color 0.2s ease-in;
}
