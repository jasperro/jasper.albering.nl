---
import { Picture } from 'astro:assets';

type Props = Parameters<typeof Picture>[0] & { caption?: string };

const { caption, ...props } = Astro.props;
---

<figure>
	<Picture formats={['avif', 'webp', 'png']} {...props} />
	{caption && <figcaption>{caption}</figcaption>}
</figure>
